-- ВАЖНО: Этот скрипт хеширует пароли существующих пользователей
-- Используйте только если пароли в БД хранятся в открытом виде
-- Для новых пользователей пароли должны хешироваться при создании

-- Функция для хеширования пароля (SHA256 в Base64)
-- В реальном приложении используйте более безопасные методы (bcrypt, Argon2 и т.д.)

-- Пример обновления пароля для пользователя (замените на реальные значения):
-- UPDATE users 
-- SET password = encode(digest('ваш_пароль', 'sha256'), 'base64')
-- WHERE email = 'user@example.com';

-- ВНИМАНИЕ: 
-- 1. Этот скрипт использует SHA256, что не является самым безопасным методом
-- 2. Рекомендуется использовать bcrypt или Argon2 для хеширования паролей
-- 3. Для продакшена рассмотрите использование Microsoft.AspNetCore.Identity

-- Пример для массового хеширования (ОСТОРОЖНО - только для тестовых данных):
-- UPDATE users 
-- SET password = encode(digest(password, 'sha256'), 'base64')
-- WHERE password IS NOT NULL 
--   AND password != '' 
--   AND password NOT LIKE '%=%'; -- Пропускаем уже захешированные пароли

