@model IEnumerable<WebApplication1.Models.DBModels.Role>
@{
    ViewData["Title"] = "Роли";
}

@section Styles {
    <link rel="stylesheet" href="~/web/kindergarten/css/detsad-clients.css" asp-append-version="true" />
}

<div class="children-page settings-page settings-subpage settings-list-page">
    <nav class="detsad-breadcrumb" aria-label="Навигация">
        <a asp-controller="Settings" asp-action="Index">Настройки</a>
        <span class="breadcrumb-sep">/</span>
        <span>Роли</span>
    </nav>

    <div class="page-header d-flex flex-wrap justify-content-between align-items-center gap-2">
        <h1 class="page-title mb-0">Роли</h1>
        <div class="d-flex flex-wrap gap-2">
            <a asp-controller="Settings" asp-action="Index" class="detsad-btn-secondary">← К настройкам</a>
            <a asp-action="Create" class="detsad-btn-primary">Создать роль</a>
        </div>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="dashboard-table-card">
            <div class="table-wrap">
                <table class="table-soft">
                    <thead>
                        <tr>
                            <th>Название</th>
                            <th>Количество прав</th>
                            <th class="text-end">Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var role in Model)
                        {
                            var permissionsCount = role.RolePermissions?.Count(rp => rp.Isdeleted == null || rp.Isdeleted == 0) ?? 0;
                            <tr>
                                <td class="cell-name">@role.Name</td>
                                <td class="cell-muted">@permissionsCount</td>
                                <td class="text-end">
                                    <div class="btn-group btn-group-sm" role="group">
                                        <a asp-action="ManagePermissions" asp-route-id="@role.Id" class="btn btn-outline-primary">Права</a>
                                        <a asp-action="Details" asp-route-id="@role.Id" class="btn btn-outline-secondary">Детали</a>
                                        <a asp-action="Edit" asp-route-id="@role.Id" class="btn btn-outline-warning">Изменить</a>
                                        <a asp-action="Delete" asp-route-id="@role.Id" class="btn btn-outline-danger">Удалить</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <div class="empty-state-settings">
            <p class="mb-0">Роли не найдены.</p>
            <a asp-action="Create" class="detsad-btn-primary mt-3 d-inline-flex">Создать первую роль</a>
        </div>
    }
</div>
