@using WebApplication1.Helpers
@using WebApplication1.Services.Cabinets.Navigation
@inject WebApplication1.Models.DBModels.AppDbContext LayoutDb
@inject ICabinetNavigationService NavigationService
@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();
    var userId = Context.Session.GetString("UserId");
    var userPerms = string.IsNullOrEmpty(userId) ? new List<string>() : PermissionHelper.GetUserPermissions(LayoutDb, userId);
    var isDoctorRole = AuthorizationHelper.IsDoctorOnlyMode(Context);
    var navItems = NavigationService.BuildMain("medclinic", userPerms, isDoctorRole);
    var footerItems = NavigationService.BuildSecondary("medclinic", userPerms, isDoctorRole);
}
<aside class="medclinic-sidebar" data-shell-sidebar>
    <div class="medclinic-sidebar__top">
        <a class="medclinic-brand medclinic-brand--logo" asp-controller="Cabinet" asp-action="Index" title="SECORE">
            <img src="~/web/kindergarten/img/secore-logo.png" alt="SECORE" class="medclinic-brand__logo-img" asp-append-version="true" />
            <span class="medclinic-brand__text">
                <strong>SECORE</strong>
                <small>Медицинский кабинет</small>
            </span>
        </a>
        <button type="button" class="app-shell-sidebar-close" data-shell-close aria-label="Закрыть меню">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M18.3 5.71a1 1 0 0 0-1.41 0L12 10.59 7.11 5.7A1 1 0 1 0 5.7 7.11L10.59 12 5.7 16.89a1 1 0 1 0 1.41 1.41L12 13.41l4.89 4.89a1 1 0 0 0 1.41-1.41L13.41 12l4.89-4.89a1 1 0 0 0 0-1.4z" fill="currentColor"/></svg>
        </button>
        <button type="button" class="medclinic-sidebar__toggle" id="medclinicSidebarToggle" aria-label="Свернуть меню" aria-expanded="true">
            <span></span>
            <span></span>
            <span></span>
        </button>
    </div>

    <nav class="medclinic-nav">
        @await Html.PartialAsync(
            "~/Views/Shared/CabinetLayouts/Partials/_CabinetSidebarNav.cshtml",
            navItems,
            new ViewDataDictionary(ViewData)
            {
                ["Variant"] = "medclinic",
                ["ActiveController"] = controller
            })
    </nav>

    @if (footerItems.Count > 0)
    {
        <div class="medclinic-sidebar__footer">
            @await Html.PartialAsync(
                "~/Views/Shared/CabinetLayouts/Partials/_CabinetSecondaryNav.cshtml",
                footerItems,
                new ViewDataDictionary(ViewData)
                {
                    ["ActiveController"] = controller,
                    ["ItemClass"] = "medclinic-nav__link",
                    ["IconClass"] = "medclinic-nav__icon",
                    ["LabelClass"] = "medclinic-nav__label",
                    ["ActiveClass"] = "active"
                })
        </div>
    }
</aside>
